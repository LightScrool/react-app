import React from 'react';
import '../styles/Logo.css';
import {Link} from "react-router-dom";

const Logo = () => {
    return (
        <Link to="" className="Logo">ReactApp</Link>
    );
};

export default Logo;